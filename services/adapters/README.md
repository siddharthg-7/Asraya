# @pramana/service-adapters

Service-level wrappers hosting legacy data adapters and transformation pipelines.

## Status

`NOT STARTED` (Setup Phase Only)

## Key Functionality

- Hosts external-facing connector processes to run batch or transactional legacy data extraction.
- Maps legacy payloads through `@pramana/adapters` into canonical attribute definitions.
