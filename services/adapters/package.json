{
  "name": "@pramana/service-adapters",
  "version": "0.1.0",
  "private": true,
  "description": "Integration service microservices hosting legacy adapters and data extraction pipelines",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc -b",
    "typecheck": "tsc --noEmit",
    "clean": "rimraf dist *.tsbuildinfo"
  },
  "dependencies": {
    "@pramana/shared": "workspace:*",
    "@pramana/schemas": "workspace:*",
    "@pramana/adapters": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.7.3"
  }
}
