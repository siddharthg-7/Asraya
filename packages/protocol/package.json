{
  "name": "@pramana/protocol",
  "version": "0.1.0",
  "description": "Canonical protocol definitions, request contracts, predicate grammar, and message formats",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc -b",
    "typecheck": "tsc --noEmit",
    "clean": "rimraf dist *.tsbuildinfo"
  },
  "dependencies": {
    "@pramana/shared": "workspace:*",
    "@pramana/schemas": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.7.3"
  }
}
