{
  "name": "@pramana/transport",
  "version": "0.1.0",
  "description": "Ephemeral transport channels, QR codes, offline payloads, and replay protection",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc -b",
    "typecheck": "tsc --noEmit",
    "clean": "rimraf dist *.tsbuildinfo"
  },
  "dependencies": {
    "@pramana/shared": "workspace:*",
    "@pramana/protocol": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.7.3"
  }
}
