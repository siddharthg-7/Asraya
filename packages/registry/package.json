{
  "name": "@pramana/registry",
  "version": "0.1.0",
  "description": "Trust registry client, institution identity resolution, and checkpoint verification",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc -b",
    "typecheck": "tsc --noEmit",
    "clean": "rimraf dist *.tsbuildinfo"
  },
  "dependencies": {
    "@pramana/shared": "workspace:*",
    "@pramana/protocol": "workspace:*",
    "@pramana/schemas": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.7.3"
  }
}
