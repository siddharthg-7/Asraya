{
  "name": "@pramana/shared",
  "version": "0.1.0",
  "description": "Shared utility types, constants, and error primitives across Pramāṇa workspace",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc -b",
    "typecheck": "tsc --noEmit",
    "clean": "rimraf dist *.tsbuildinfo"
  },
  "devDependencies": {
    "typescript": "^5.7.3"
  }
}
