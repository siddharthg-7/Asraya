{
  "name": "@pramana/test-fixtures",
  "version": "0.1.0",
  "description": "Standardized test vectors, synthetic mock fixtures, and conformance test datasets",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc -b",
    "typecheck": "tsc --noEmit",
    "clean": "rimraf dist *.tsbuildinfo"
  },
  "dependencies": {
    "@pramana/shared": "workspace:*",
    "@pramana/schemas": "workspace:*",
    "@pramana/protocol": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.7.3"
  }
}
