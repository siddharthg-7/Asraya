{
  "name": "@pramana/registry-app",
  "version": "0.1.0",
  "private": true,
  "description": "Trust Registry Service Application (Schema repository and institution DID registry)",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc -b",
    "typecheck": "tsc --noEmit",
    "clean": "rimraf dist *.tsbuildinfo"
  },
  "dependencies": {
    "@pramana/shared": "workspace:*",
    "@pramana/protocol": "workspace:*",
    "@pramana/schemas": "workspace:*",
    "@pramana/registry": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.7.3"
  }
}
